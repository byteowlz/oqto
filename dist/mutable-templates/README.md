# dist/mutable-templates/

Seed templates for mutable files.

These files are copied once into mutable locations (e.g. `/etc/oqto`, user-local config), then treated as user/admin owned and never overwritten silently by release updates.
