# onboarding templates

Embedded fallback onboarding templates shipped with Oqto.

This mirrors the currently embedded files in `backend/crates/oqto/src/templates/embedded/*`
and uses the same subdirectory contract (`onboarding/`) as the template service config.
