# dist/immutable/defaults/pi-agent/

Shipped baseline content for `~/.pi/agent` layering.

Typical files:
- `AGENTS.md` baseline
- default extension/skill payloads (platform-owned)
- default settings seeds

Policy:
- platform-owned immutable defaults may be linked where appropriate
- user-editable files should be copied-once into mutable locations
