schema = "oqto-app/v0"
id = "hello-oqto"
version = "0.1.0"
title = { en = "Hello Oqto", de = "Hallo Oqto" }
description = "Zero-capability acceptance App for the first runtime slice"
presentations = ["sandboxed-web"]
requested_capabilities = []
bindings = ["work-directory"]
default_binding = "work-directory"

[presentation.sandboxed-web]
entry = "bundle/index.html"

[instance_state]
versioned = false

[assets]
max_bytes = 100000
