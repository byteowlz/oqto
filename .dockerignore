# Ignore everything
*

# Allow only what's needed for the container build
!fileserver/Cargo.toml
!fileserver/Cargo.lock
!fileserver/src/

!container/entrypoint.sh

# Note: container/skel/ is NOT included - it's used by the backend at runtime,
# not during Docker image build. User homes are mounted, not baked into the image.
